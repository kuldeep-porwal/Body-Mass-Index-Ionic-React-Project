import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'body-mass-index',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
